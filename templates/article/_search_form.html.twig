
{{ form_start(form) }}
<div class="row">
    <div class="row align-items-center">
        <div class="col-md-3">{{ form_row(form.minPrice) }} </div>
        <div class="col-md-3">{{ form_row(form.maxPrice) }} </div>
        <div class="col-md-3">{{ form_row(form.category) }} </div>
        <div class="col mt-n3">
            <div class="form-group">
                <button type="submit" class="btn btn-info"><i class="fa fa-arrow-down"></i> Rechercher</button>
                <a href="{{ path('article_index') }}" class="btn btn-secondary"><i class="fa fa-refresh"></i> Réinitialiser</a>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}